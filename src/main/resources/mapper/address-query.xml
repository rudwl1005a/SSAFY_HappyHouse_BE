<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="com.mycom.happyHouse.dao.AddressDao">

    <select id="getSido" resultType="com.mycom.happyHouse.dto.house.SidoGugunDongDto">
		select left(sidoCode,2) sidoCode, sidoName
		from sidocode
		order by sidoCode
	</select>
	
	<select id="getGugunInSido" parameterType="string" resultType="com.mycom.happyHouse.dto.house.SidoGugunDongDto">
		select left(gugunCode,5) gugunCode, gugunName
		from guguncode
		where left(gugunCode,2) = #{sido}
		order by gugunCode
	</select>
	
	<select id="getDongInGugun" parameterType="string" resultType="com.mycom.happyHouse.dto.house.SidoGugunDongDto">
		select dongName, dongCode
		from dongcode
		where left(dongCode, 5) = #{gugun}
		order by dongName
	</select>
	
</mapper>